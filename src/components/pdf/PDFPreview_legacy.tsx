// src/components/pdf/PDFPreview_legacy.tsx
// LEGACY DÉSACTIVÉ — Conservé uniquement pour référence.
// Toute utilisation directe doit être supprimée des écrans.

export default function PDFPreviewLegacy() {
  return (
    <div className="p-4 text-sm text-warning-foreground bg-warning-light rounded-md">
      Legacy PDF désactivé. Merci d'utiliser le pipeline unifié (layouts JSON).
    </div>
  );
}